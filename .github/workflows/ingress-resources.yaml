name: ingress-resources (CD)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    environment: Default

    steps:
    - name: Configure kubectl
      run: echo "${{ secrets.KUBECONFIG }}" | base64 --decode > $HOME/.kube/config

    - name: Deploy with Helm
      run: |
        helm upgrade --install nginx-gateway helm-chart/ingress-resources \
        --namespace ingress-nginx