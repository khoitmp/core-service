# Main settings
mode: ContinuousDeployment
branches:
  main:
    versioningScheme: Minor
    tag: ''
    increment: Minor

# Optional settings
assemblyVersioningScheme: MajorMinor
semanticVersioning: true
commitMessageIncrementing: Enabled
ignore:
  sha: 
    - 'a1b2c3d4e5'